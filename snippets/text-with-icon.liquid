<div class="text-with-icon {% if link %}is-link-wrapper{% endif %}"{% if block_attributes %} {{ block_attributes }}{% endif %}>
    <div {% if link %}class="is-link"{% endif %}>

    {% if link %}
        <a href={{ link }} class="link">
    {% endif %}

    {% assign iconSize = settings.typography-body-font-size | remove: "px" | plus: 2 %}
    <img src="{{ collection.metafields.custom.icon_1 }}" width=" 18" height="18" fill="#414042">
    <span>{{ collection.metafields.custom.icon_text_1 }}</span>
    {% if link %}
        </a>
    {% endif %}

    </div>
</div>
